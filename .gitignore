# Environment
.env

# macOS
.DS_Store

# Node
node_modules/

# Turborepo
.turbo/

# Separate repos (not part of monorepo)
keyway-admin/
keyway-landing/
keyway-action/

# Legacy cloned repos (pre-monorepo, can be deleted)
/keyway-backend/
/keyway-dashboard/
/keyway-crypto/
/keyway-docs/
/keyway-mcp/
/cli/

# Docker Compose override (for private services in local dev)
docker-compose.override.yml

# Docker volumes (if any local state)
caddy_data/
caddy_config/
postgres_data/

# Local development certificates
certs/
