# Environment
.env

# macOS
.DS_Store

# Node
node_modules/

# Turborepo
.turbo/

# Private/separate repos (not part of monorepo)
keyway-admin/
keyway-landing/
keyway-action/

# Docker Compose override (for private services in local dev)
docker-compose.override.yml

# Docker volumes (if any local state)
caddy_data/
caddy_config/
postgres_data/

# Local development certificates
certs/
