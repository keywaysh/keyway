# ===========================================
# Keyway - Docker Compose Environment
# ===========================================
# Copy this file to .env in the parent directory and fill in your values:
#   cp .env.example ../.env
#
# Generate secure keys with:
#   openssl rand -hex 32    (for ENCRYPTION_KEY)
#   openssl rand -base64 32 (for JWT_SECRET)
# ===========================================

# -----------------------------------------
# Encryption Key (CRITICAL)
# -----------------------------------------
# AES-256-GCM encryption key for secrets at rest
# Must be a 64-character hexadecimal string (32 bytes)
#
# WARNING: Once secrets are stored, this key CANNOT be changed
# without losing all encrypted data!
#
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# -----------------------------------------
# JWT Secret
# -----------------------------------------
# Used to sign authentication tokens
# Must be at least 32 characters
#
# Generate with: openssl rand -base64 32
JWT_SECRET=

# -----------------------------------------
# GitHub App (Required)
# -----------------------------------------
# Create a GitHub App at: https://github.com/settings/apps/new
#
# Settings:
#   - App name: keyway-local-dev (or any unique name)
#   - Homepage URL: https://localhost
#   - Callback URL: https://localhost/auth/callback
#   - Setup URL: (leave empty)
#   - Webhook URL: https://localhost/v1/github/webhook (optional for local)
#   - Webhook secret: (optional for local)
#
# Permissions:
#   - Repository permissions > Metadata: Read-only
#
# After creating:
#   1. Note the App ID and Client ID
#   2. Generate a client secret
#   3. Generate a private key (download .pem file)
#   4. Convert private key to single line:
#      awk 'NF {sub(/\r/, ""); printf "%s\\n",$0;}' your-app.pem
#
GITHUB_APP_ID=
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=
GITHUB_APP_PRIVATE_KEY=
GITHUB_APP_WEBHOOK_SECRET=
GITHUB_APP_NAME=keyway-local-dev

# -----------------------------------------
# Optional: Sentry Error Tracking
# -----------------------------------------
# NEXT_PUBLIC_SENTRY_DSN=
